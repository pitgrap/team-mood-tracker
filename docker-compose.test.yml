services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: moodtracker_test
    ports:
      - '5433:5432'
    tmpfs:
      - /var/lib/postgresql/data
